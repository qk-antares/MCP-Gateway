[project]
name = "mcp-gateway"
version = "0.1.0"
description = "MCP Server Gateway 网关"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "aiosqlite>=0.20.0",
    "pydantic>=2.0.0",
    "mcp[cli]>=1.27.1",
]
